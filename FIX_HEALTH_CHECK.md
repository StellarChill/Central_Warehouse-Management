# 🔧 แก้ปัญหา Health Check - "Not Found"

## ❌ ปัญหา

เมื่อเปิด `/health` endpoint แล้วเห็น "Not Found"

**สาเหตุที่เป็นไปได้:**
1. ใช้ URL placeholder (`your-backend-url`) แทน URL จริง
2. Backend service ยังไม่รันหรือมี error
3. Route `/health` ไม่ทำงาน

---

## ✅ วิธีแก้ไข

### 1. หา URL จริงของ Backend Service

**วิธีหา:**
1. ไปที่ **Render Dashboard**
2. เข้าไปที่ **Backend Service** (`Central_Warehouse-Management-backend`)
3. ดูที่ **URL** ด้านบนของหน้า (มักจะเป็น `https://xxx.onrender.com`)
4. **คัดลอก URL นี้** (ไม่ต้องมี `/health`)

**ตัวอย่าง:**
- `https://central-warehouse-management-backend.onrender.com`

---

### 2. ทดสอบ Health Check ด้วย URL จริง

**เปิดใน browser:**
```
https://your-actual-backend-url.onrender.com/health
```

**ใช้ URL จริงที่คุณคัดลอกมา** แทน `your-backend-url`

---

### 3. ตรวจสอบว่า Backend Service รันอยู่

**ตรวจสอบ:**
1. ไปที่ **Backend Service** → **Logs**
2. ดูว่ามี error หรือไม่
3. ควรเห็น: `✅ Server running on http://localhost:3000`

**ถ้ามี error:**
- ดู error message และแก้ไขตามนั้น
- ตรวจสอบ Environment Variables (`DATABASE_URL`, etc.)

---

### 4. ตรวจสอบว่า Build สำเร็จ

**ตรวจสอบ:**
1. ไปที่ **Backend Service** → **Logs**
2. ดูว่ามี Build logs
3. ควรเห็น: `All migrations have been successfully applied`
4. ควรเห็น: `Build succeeded`

**ถ้า Build ล้มเหลว:**
- ตรวจสอบ Build Command
- ตรวจสอบ logs สำหรับ error

---

## 🔍 วิธีตรวจสอบ

### ถ้า URL ถูกต้องแล้วแต่ยัง "Not Found"

**ลองทดสอบ:**

1. **เปิด root endpoint:**
   ```
   https://your-backend-url.onrender.com/
   ```
   ควรเห็น: `{"message":"Sai Jai Management API",...}`

2. **ถ้า root endpoint ทำงาน แต่ `/health` ไม่ทำงาน:**
   - ตรวจสอบว่า `backend/src/server.ts` มี route `/health` หรือไม่
   - ตรวจสอบ logs ว่ามี error อะไร

3. **ถ้าทั้ง root และ `/health` ไม่ทำงาน:**
   - Backend service อาจไม่รัน
   - ตรวจสอบ logs และ error messages

---

## 📋 Checklist

- [ ] หา URL จริงของ Backend Service
- [ ] ทดสอบ `/health` ด้วย URL จริง
- [ ] ตรวจสอบว่า Backend Service รันอยู่ (ดู Logs)
- [ ] ตรวจสอบว่า Build สำเร็จ
- [ ] ทดสอบ root endpoint `/` ก่อน

---

## 🐛 Troubleshooting

### ถ้ายังเห็น "Not Found"

**ตรวจสอบ:**
1. URL ใช้ URL จริงหรือยัง (ไม่ใช่ placeholder)
2. Backend service status เป็น "Live" หรือ "Running"
3. Logs มี error หรือไม่
4. Environment Variables ตั้งค่าครบหรือไม่ (`DATABASE_URL`)

### ถ้าเห็น "Cannot GET /health"

**ตรวจสอบ:**
1. Backend service รันอยู่จริงหรือไม่
2. Route `/health` อยู่ใน `backend/src/server.ts` หรือไม่
3. Build สำเร็จและสร้างไฟล์ `dist/server.js` หรือไม่

---

## 💡 หมายเหตุ

**URL ที่ถูกต้อง:**
- ✅ `https://central-warehouse-management-backend.onrender.com/health`
- ❌ `https://your-backend-url.onrender.com/health` (placeholder)

**ตรวจสอบ:**
- ใช้ URL จริงจาก Render Dashboard
- ไม่ใช่ placeholder text

---

**ลองใช้ URL จริงจาก Render Dashboard แล้วทดสอบ `/health` อีกครั้ง!**

