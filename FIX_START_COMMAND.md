# 🔧 แก้ปัญหา "Missing script: start"

## ❌ ปัญหา

```
npm error Missing script: "start"
```

**สาเหตุ:** Render ไม่พบ `package.json` ที่มี script "start" น่าจะเป็นเพราะ **Root Directory ไม่ถูกต้อง**

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: ตรวจสอบและแก้ไข Root Directory (แนะนำ)

#### ขั้นตอน:

1. **เข้าไปที่ Backend Service** → **Settings**

2. **หาส่วน "Root Directory"**

3. **ตรวจสอบว่าตั้งค่าอะไรไว้:**
   - ถ้าเป็น `/` หรือว่าง → **เปลี่ยนเป็น** `backend` ✅
   - ถ้าเป็น `backend` แล้ว → ไปที่วิธีที่ 2

4. **เปลี่ยน Root Directory เป็น:**
   ```
   backend
   ```

5. **ตรวจสอบ Start Command:**
   - ควรเป็น: `npm start` (default)
   - หรือ: `node dist/server.js`

6. **Save และ Deploy ใหม่**

---

### วิธีที่ 2: แก้ไข Start Command (ถ้า Root Directory = `/`)

ถ้า Root Directory ต้องเป็น `/` (root ของ repo):

#### ขั้นตอน:

1. **เข้าไปที่ Backend Service** → **Settings**

2. **หาส่วน "Start Command"**

3. **เปลี่ยนจาก:**
   ```
   npm start
   ```
   
   **เป็น:**
   ```
   cd backend && npm start
   ```

4. **หรือ:**
   ```
   cd backend && node dist/server.js
   ```

5. **Save และ Deploy ใหม่**

---

### วิธีที่ 3: ใช้ Absolute Path

ถ้าวิธีอื่นไม่ได้ผล:

#### ขั้นตอน:

1. **เข้าไปที่ Backend Service** → **Settings** → **Start Command**

2. **เปลี่ยนเป็น:**
   ```bash
   cd /opt/render/project/src/backend && npm start
   ```

3. **Save และ Deploy ใหม่**

---

## 🔍 วิธีตรวจสอบ

### ตรวจสอบว่า Root Directory ถูกต้อง:

1. **เข้าไปที่ Backend Service** → **Settings**
2. **ดู "Root Directory"**
3. **ควรเป็น:** `backend`

### ตรวจสอบว่า package.json มี script "start":

Script "start" ควรอยู่ใน `backend/package.json`:
```json
{
  "scripts": {
    "start": "node dist/server.js"
  }
}
```

---

## 📋 การตั้งค่าที่แนะนำ

### การตั้งค่าที่ถูกต้อง:

**Root Directory:** `backend`

**Build Command:**
```bash
npm install && npx prisma generate && npx prisma migrate deploy && npm run build
```

**Start Command:**
```
npm start
```
หรือ
```
node dist/server.js
```

---

## ✅ Checklist

- [ ] Root Directory = `backend`
- [ ] Start Command = `npm start` หรือ `node dist/server.js`
- [ ] Save และ Deploy ใหม่
- [ ] ตรวจสอบ Logs ว่า service รันได้แล้ว

---

## 🐛 Troubleshooting

### ถ้ายังเห็น "Missing script: start"

**ลอง:**
1. ตรวจสอบว่า Root Directory = `backend` จริงๆ
2. ตรวจสอบว่า `backend/package.json` มี script "start"
3. ลองใช้ Start Command แบบ absolute:
   ```
   cd /opt/render/project/src/backend && npm start
   ```

### ถ้า Build สำเร็จแต่ Start ล้มเหลว

**ตรวจสอบ:**
1. Build Command รันสำเร็จหรือไม่ (ดูใน Logs)
2. ไฟล์ `dist/server.js` ถูกสร้างหรือไม่
3. Environment variables ตั้งค่าครบหรือไม่ (`DATABASE_URL`, etc.)

---

## 💡 คำแนะนำ

**แนะนำ:** ตั้ง Root Directory = `backend` แล้วใช้ Start Command = `npm start`

วิธีนี้สะอาดและเข้าใจง่าย!

