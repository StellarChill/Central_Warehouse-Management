# ✅ Post-Deployment Checklist - หลัง Deploy แล้วต้องทำอะไรต่อ

## 🎯 ขั้นตอนหลัง Deploy

### 1. ✅ ตรวจสอบ Backend Service

#### 1.1 ตรวจสอบว่า Backend รันอยู่
- เข้าไปที่ Backend Service dashboard
- ดูว่า service status เป็น "Running" หรือ "Deployed"
- ตรวจสอบ logs ว่ามี error หรือไม่

#### 1.2 ทดสอบ Health Check Endpoint
เปิดเบราว์เซอร์หรือใช้ curl ทดสอบ:

```
https://your-backend-url.onrender.com/health
```

**ผลลัพธ์ที่ควรเห็น:**
```json
{
  "status": "ok",
  "database": "connected",
  "timestamp": "2025-01-XX..."
}
```

**ถ้าเห็น `"database": "disconnected"`:**
- ❌ ตรวจสอบ `DATABASE_URL` ว่าตั้งค่าถูกต้องหรือไม่
- ❌ ตรวจสอบว่า PostgreSQL service (`sai-jaimange`) รันอยู่หรือไม่

---

### 2. ✅ ตั้งค่า Environment Variables

#### 2.1 Backend Service Environment Variables

ตรวจสอบว่ามีครบหรือไม่:

- [ ] `DATABASE_URL` - Connection string จาก PostgreSQL service
- [ ] `JWT_SECRET` - Secret key สำหรับ authentication
- [ ] `FRONTEND_URL` - URL ของ frontend service
- [ ] `PORT` - (มักจะกำหนดโดย platform อัตโนมัติ)

**วิธีตรวจสอบ:**
1. เข้าไปที่ Backend Service → Environment หรือ Config Vars
2. ตรวจสอบว่ามี variables ครบตามด้านบน

**ถ้ายังไม่มี ให้เพิ่ม:**
- `DATABASE_URL`: ไปที่ PostgreSQL service → Settings → Copy Connection String
- `JWT_SECRET`: สร้าง random string (เช่น: `openssl rand -hex 32`)
- `FRONTEND_URL`: URL จริงของ frontend service

#### 2.2 Frontend Service Environment Variables

ตรวจสอบว่ามี:

- [ ] `VITE_API_URL` - URL ของ backend + `/api`

**วิธีตรวจสอบ:**
1. เข้าไปที่ Frontend Service → Environment หรือ Config Vars
2. ตรวจสอบว่ามี `VITE_API_URL` หรือไม่

**ถ้ายังไม่มี ให้เพิ่ม:**
- `VITE_API_URL`: `https://your-backend-url.onrender.com/api`
- ⚠️ **สำคัญ:** ต้อง rebuild frontend หลังตั้งค่า!

---

### 3. ✅ รัน Database Migrations

ถ้าเป็นครั้งแรกที่ deploy ต้องรัน migrations:

**วิธีที่ 1: ผ่าน Render Shell**
1. เข้าไปที่ Backend Service → Shell
2. รันคำสั่ง:
   ```bash
   cd backend
   npx prisma migrate deploy
   ```

**วิธีที่ 2: ผ่าน Local (ถ้าเชื่อมต่อ database ได้)**
```bash
cd backend
npx prisma migrate deploy
```

**วิธีที่ 3: เพิ่มใน Build Command**
ใน Backend Service settings → Build Command:
```bash
npm install && npx prisma generate && npx prisma migrate deploy
```

---

### 4. ✅ ทดสอบ Frontend เชื่อมต่อ Backend

#### 4.1 ตรวจสอบ Browser Console
1. เปิดเว็บไซต์ frontend
2. กด F12 เปิด Developer Tools
3. ไปที่ tab **Console**
4. ดูว่ามี error อะไรหรือไม่

**Error ที่อาจเจอ:**
- `CORS error` → Backend ต้องเพิ่ม frontend URL ใน CORS settings
- `404 Not Found` → `VITE_API_URL` ตั้งค่าผิด หรือต้อง rebuild frontend
- `Network Error` → Backend service ไม่ได้รัน หรือ URL ผิด

#### 4.2 ตรวจสอบ Network Requests
1. ใน Developer Tools → tab **Network**
2. ลอง login หรือดูข้อมูลในเว็บ
3. ดูว่ามี requests ไปที่ backend API หรือไม่
4. ตรวจสอบว่า response เป็น success (200) หรือมี error

**ตัวอย่าง URL ที่ควรเห็น:**
```
https://your-backend-url.onrender.com/api/login
https://your-backend-url.onrender.com/api/branch
https://your-backend-url.onrender.com/api/catagory
```

---

### 5. ✅ ทดสอบการ Login และใช้งาน

#### 5.1 ทดสอบ Login
1. เปิดเว็บไซต์ frontend
2. ไปที่หน้า Login
3. กรอก username และ password
4. คลิก "เข้าสู่ระบบ"

**ถ้า login สำเร็จ:**
- ✅ จะ redirect ไปหน้า Dashboard
- ✅ เห็นชื่อผู้ใช้ที่มุมขวาบน
- ✅ สามารถเข้าหน้าอื่นๆ ได้

**ถ้า login ไม่ได้:**
- ตรวจสอบ Browser Console ว่ามี error อะไร
- ตรวจสอบ Network tab ว่า API call ส่งไปถึง backend หรือไม่
- ตรวจสอบ backend logs ว่ามี error หรือไม่

#### 5.2 ทดสอบดูข้อมูลจาก Database
1. หลัง login สำเร็จ
2. ลองเปิดหน้าต่างๆ เช่น:
   - Dashboard
   - Categories
   - Branches
   - Materials
3. ดูว่าข้อมูลแสดงผลหรือไม่

---

### 6. 🔍 Troubleshooting

#### ❌ ปัญหา: Backend Health Check แสดง `"database": "disconnected"`

**แก้ไข:**
1. ตรวจสอบ `DATABASE_URL` ใน backend environment variables
2. ตรวจสอบว่า PostgreSQL service (`sai-jaimange`) รันอยู่
3. ตรวจสอบ connection string ว่าถูกต้องหรือไม่
4. ลองใช้ internal network URL แทน external URL

#### ❌ ปัญหา: Frontend เรียก API แล้วได้ CORS Error

**แก้ไข:**
1. ตรวจสอบว่า `FRONTEND_URL` ใน backend ตั้งค่าเป็น URL จริงของ frontend
2. ตรวจสอบ `backend/src/server.ts` ว่า frontend URL อยู่ใน `allowedOrigins`
3. Restart backend service

#### ❌ ปัญหา: Frontend เรียก API แล้วได้ 404

**แก้ไข:**
1. ตรวจสอบ `VITE_API_URL` ว่าตั้งค่าถูกต้อง (ต้องมี `/api` ตอนท้าย)
2. **สำคัญ:** ต้อง rebuild frontend service หลังตั้งค่า `VITE_API_URL`
3. ตรวจสอบว่า backend routes ทำงานถูกต้อง

#### ❌ ปัญหา: Frontend แสดงข้อมูลไม่ได้

**แก้ไข:**
1. ตรวจสอบ backend logs ว่ามี error หรือไม่
2. ตรวจสอบ database migrations ว่ารันแล้วหรือยัง
3. ตรวจสอบว่ามีข้อมูลใน database หรือไม่
4. ทดสอบ API โดยตรงผ่าน Postman หรือ curl

---

## 📝 Quick Test Commands

### ทดสอบ Backend Health
```bash
curl https://your-backend-url.onrender.com/health
```

### ทดสอบ Backend API (Login)
```bash
curl -X POST https://your-backend-url.onrender.com/api/login \
  -H "Content-Type: application/json" \
  -d '{"UserName":"your-username","UserPassword":"your-password"}'
```

### ทดสอบ Database Connection (ผ่าน Backend)
```bash
curl https://your-backend-url.onrender.com/health
# ดูว่า "database": "connected" หรือไม่
```

---

## ✅ Final Checklist

หลังทำครบทุกขั้นตอนแล้ว ตรวจสอบว่า:

- [ ] Backend service รันอยู่และ health check แสดง `"database": "connected"`
- [ ] Environment variables ตั้งค่าครบ (ทั้ง backend และ frontend)
- [ ] Database migrations รันแล้ว
- [ ] Frontend สามารถเรียก backend API ได้ (ดูใน Network tab)
- [ ] Login ได้และสามารถใช้งานระบบได้ปกติ
- [ ] ข้อมูลจาก database แสดงผลใน frontend ได้

---

## 🎉 เสร็จแล้ว!

ถ้าทุกอย่างผ่านหมด แสดงว่า deployment สำเร็จแล้ว! 

**สิ่งที่ควรทำต่อไป:**
- ตั้งค่า monitoring และ alerts
- Backup database เป็นประจำ
- ตั้งค่า SSL certificate (ถ้ายังไม่มี)
- ตรวจสอบ performance และ optimize ถ้าจำเป็น

---

**คำถามหรือปัญหาเพิ่มเติม?** ตรวจสอบ logs ของ services เพื่อดู error messages ที่เจาะจงกว่า

